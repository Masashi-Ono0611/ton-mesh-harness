/**
 * Public re-exports for the v0.8 SDK.
 *
 * - `schemas`: zod source of truth for all I/O contracts ([F2] #13).
 * - `check`: programmatic environment probe ([S1] #6).
 * - `deploy`: runtime deploy SDK — coming in [S2] #7.
 */

export * from './schemas'
export * from './check'
